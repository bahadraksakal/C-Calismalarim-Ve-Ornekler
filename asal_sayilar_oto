#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,j;

    for(j=1 ; j<=50;j++)
    {
        for (i=2;i<j;i++)
        {
            if (j % i == 0)
                break;
        }
        if (j == i)
            printf("asal %d sayidir\n",j);

    }
    return 0;
}
